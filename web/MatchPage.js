function swapPreUser(one,two){
	one.setAttribute("class", "hide");
	two.setAttribute("class", "show");
}

function swapNextUser(one,two){
	one.setAttribute("class", "hide");
	two.setAttribute("class", "show");
}